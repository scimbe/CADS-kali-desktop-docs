# Reference

Look-up material. Dry and complete; no narrative.

- **[Environment variables](environment-variables.md)** — every var in `.env.template` and
  `.env.tunnel.template`, plus the ones set directly in the compose files.
- **[Container and port layout](container-and-port-layout.md)** — every container, its image,
  network, and exposed (never published) ports.
